# Constitution Principle I: the gate runs identically here and in CI, and CI is the authority.
pre-commit:
  parallel: false
  commands:
    format:
      run: npm run format:check
    lint:
      run: npm run lint
    typecheck:
      run: npm run typecheck
    test:
      run: npm test
